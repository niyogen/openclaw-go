# Build artefacts — only the compiled binaries, not source directories
dist/
dist/openclaw
dist/openclaw.exe

# Local data and secrets (never commit)
*.env
.env.*

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
*.swo
