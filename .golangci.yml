version: "2"

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosimple
    - errcheck
    - gofmt
    - goimports

linters-settings:
  gofmt:
    simplify: true
  goimports:
    local-prefixes: openclaw-go

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  exclude-rules:
    # Allow TODOs in test files.
    - path: _test\.go
      linters: [errcheck]
